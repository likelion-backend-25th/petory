info:
  name: 회원조회
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/profile/2"
  auth:
    type: basic
    username: mungchi@petory.com
    password: password123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
